const CreditTable = ({ data }) => {
  const { amount,
    creditType,
    loanDate,
    mainPercent,
    years, 
  } = data;

  
  console.log(data);

  return (<div>
    1
  </div>)
};

export default CreditTable;